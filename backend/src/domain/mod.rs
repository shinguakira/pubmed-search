//! Pure logic: no IO, no DB, no HTTP. Unit-testable without spinning up
//! NCBI or the server. Add modules here for citation rendering, query
//! construction, validation helpers, etc.

pub mod citation;
